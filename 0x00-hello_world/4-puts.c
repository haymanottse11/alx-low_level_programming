#include <stdio.h>
/**
 * main -entry value
 * not allowed to use printf allowed only to use puts
(*
 * Return 0 on success   
 */
int main(void)
{
char ques4[] = "\"Programming is like building a multilingual puzzle";

puts(ques4);
return (0);
}
